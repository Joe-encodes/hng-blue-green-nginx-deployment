# Environment variables for the Blue/Green Nginx deployment with observability.
# These values are used by docker-compose.yml and watcher.py.

# --- Application Images ---
# Docker image references for the Blue and Green Node.js application services.
# These images are pre-built and contain the application code.
BLUE_IMAGE=
GREEN_IMAGE=

# --- Release_ID Configuration ---
RELEASE_ID_BLUE=v1.0-blue
RELEASE_ID_GREEN=v1.0-green

# --- Active Pool Configuration ---
# Specifies which application pool (blue or green) should be initially active.
# This is used by Nginx for primary routing and by the watcher for initial state.
ACTIVE_POOL=blue

# --- Slack Integration ---
# The Slack Incoming Webhook URL to send alerts to.
# IMPORTANT: Replace with your actual Slack webhook URL.
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/YOUR/WEBHOOK/HERE

# --- Alerting Thresholds and Cooldowns ---
# ERROR_RATE_THRESHOLD: Percentage of 5xx errors over the window_size that triggers an alert.
# WINDOW_SIZE: Number of most recent requests to consider for error rate calculation.
# ALERT_COOLDOWN_SEC: Time in seconds to wait between sending similar alerts to prevent spam.
ERROR_RATE_THRESHOLD=2
WINDOW_SIZE=200
ALERT_COOLDOWN_SEC=300
